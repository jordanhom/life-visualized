# Changes — 2026-03-24

Summary:

- Upgraded dev tooling dependencies to remediate open Dependabot alerts in transitive dev paths:
  - `vitest` -> `^4.1.1`
  - `jsdom` -> `^29.0.1`
  - `c8` -> `^11.0.0`
- Regenerated lockfile and validated local security posture with `npm audit` (0 vulnerabilities reported locally after update).
- Added CI quality gates and scripts:
  - `npm run lint`
  - `npm run typecheck`
  - `npm run verify`
- Added ESLint flat config and TypeScript no-emit typecheck config:
  - [`eslint.config.js`](eslint.config.js:1)
  - [`tsconfig.json`](tsconfig.json:1)
  - [`types/globals.d.ts`](types/globals.d.ts:1)
- Updated GitHub Actions workflow to Node 20 and to run lint + typecheck + tests:
  - [`.github/workflows/ci.yml`](.github/workflows/ci.yml:1)
- Updated tests for Vitest 4 mock semantics by switching relevant cases to `vi.doMock(...)`.

Notes:

- The app runtime remains a static browser SPA with CDN-loaded `date-fns` / `date-fns-tz`; dependency upgrades in this change are test/dev-tooling focused.
- Dependabot alerts in GitHub will clear after this state is present on the default branch and alerts are re-evaluated server-side.
