# Changes — 2026-03-25

Summary:

- CI workflow updated to run a Node.js version matrix on `20.x` and `22.x`.
- CI job simplified to run a single gate command: `npm run verify` (lint + typecheck + tests).
- ESLint test policy tightened:
  - Re-enabled `no-unused-vars` in test files with underscore ignore patterns for intentional placeholders.
  - Scoped `no-empty` relaxation to `tests/setup/jsdom-helper.js` only.
- Applied minimal test cleanup to match lint policy (unused arg/catch binding naming).
- Expanded and refined unit-test coverage across `ui`, `calculator`, and `gridRenderer` suites.
- Removed low-value/duplicate tests:
  - Deleted debug-only `tests/unit/data-inspect.test.js`.
  - Removed duplicate `nonbinary` invalid-sex assertion in `tests/unit/calculator.test.js`.

Notes:

- These changes improve signal quality in linting without introducing broad test-file churn.
- Local validation completed via `npm run verify` with all tests passing.
- Current local test baseline (after cleanup): `11` test files, `67` tests passing.
- Current local coverage baseline:
  - Statements: `96.32%`
  - Branches: `78.84%`
  - Functions: `100%`
  - Lines: `97.8%`
