# Changes — 2026-01-23

Summary:

- Added timezone handling improvements to the grid renderer. The renderer now uses date-fns-tz when available (via CDN) and falls back to core date-fns when not available. Files changed: [`js/gridRenderer.js`](js/gridRenderer.js:1), [`index.html`](index.html:139).
- Added `date-fns-tz` CDN to the page to provide timezone-aware helpers for formatting and UTC conversions.
- Ran the unit test suite (44 tests) — all tests passed locally.
- Created and merged a PR containing the WIP branch into `main`: PR [#22](https://github.com/jordanhom/life-visualized/pull/22).

Notes:

- This change is primarily a runtime robustness improvement — it avoids passing unsupported `timeZone` options to core date-fns and uses `utcToZonedTime` / `formatInTimeZone` when available.
- Follow-up items were opened as issues prior to the merge; see the memory bank update for links and next steps.

